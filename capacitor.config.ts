import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.paranoid.android.boykott',
  appName: 'Paranoid Android Boykott App',
  webDir: 'dist'
};

export default config;
